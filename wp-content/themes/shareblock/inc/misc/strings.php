<?php
class shareblocktxt {
	public static function shareblock_s_by() {
		$string = get_theme_mod('shareblock_s_by');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'By', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_mins_read() {
		$string = get_theme_mod('shareblock_s_mins_read');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Mins read', 'shareblock' );
		}
		return $string;
	}
	
	public static function shareblock_s_views() {
		$string = get_theme_mod('shareblock_s_views');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Views', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_shares() {
		$string = get_theme_mod('shareblock_s_shares');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Shares', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_share_on_facebook() {
		$string = get_theme_mod('shareblock_s_share_on_facebook');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Share on Facebook', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_share_on_twitter() {
		$string = get_theme_mod('shareblock_s_share_on_twitter');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Share on Twitter', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_previous_post() {
		$string = get_theme_mod('shareblock_s_previous_post');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Previous post', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_next_post() {
		$string = get_theme_mod('shareblock_s_next_post');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Next post', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_related_articles() {
		$string = get_theme_mod('shareblock_s_related_articles');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Related Articles', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_older_comments() {
		$string = get_theme_mod('shareblock_s_older_comments');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( '« Older Comments', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_newer_comments() {
		$string = get_theme_mod('shareblock_s_newer_comments');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Newer Comments »', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_one_ping() {
		$string = get_theme_mod('shareblock_s_one_ping');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'One Ping', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_one_comment() {
		$string = get_theme_mod('shareblock_s_one_comment');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'One Comment', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_comments() {
		$string = get_theme_mod('shareblock_s_comments');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Comments', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_pings_trackbacks() {
		$string = get_theme_mod('shareblock_s_pings_trackbacks');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Pings & Trackbacks', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_fullname() {
		$string = get_theme_mod('shareblock_s_fullname');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Full Name', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_email_address() {
		$string = get_theme_mod('shareblock_s_email_address');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Email Address', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_web_url() {
		$string = get_theme_mod('shareblock_s_web_url');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Web URL', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_comment() {
		$string = get_theme_mod('shareblock_s_comment');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Comment', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_summary() {
		$string = get_theme_mod('shareblock_s_summary');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Summary', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_the_pros() {
		$string = get_theme_mod('shareblock_s_the_pros');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'The Pros', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_the_cons() {
		$string = get_theme_mod('shareblock_s_the_cons');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'The Cons', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_search() {
		$string = get_theme_mod('shareblock_s_search');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Search ...', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_search_for() {
		$string = get_theme_mod('shareblock_s_search_for');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Search for: ', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_no_result_found() {
		$string = get_theme_mod('shareblock_s_no_result_found');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'No result found', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_no_result_found_des() {
		$string = get_theme_mod('shareblock_s_no_result_found_des');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'It looks like nothing was found here. Please try it again.', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_articles() {
		$string = get_theme_mod('shareblock_s_articles');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Articles', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_404() {
		$string = get_theme_mod('shareblock_s_404');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( '404', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_404_desc() {
		$string = get_theme_mod('shareblock_s_404_desc');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'OOOOOPS!! The page you are looking for not exist!', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_back_to_home() {
		$string = get_theme_mod('shareblock_s_back_to_home');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'Back to Home', 'shareblock' );
		}
		return $string;
	}

	public static function shareblock_s_load_more() {
		$string = get_theme_mod('shareblock_s_load_more');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'load more', 'shareblock' );
		}
		return $string;
	}
	
	public static function shareblock_s_no_post_to_load() {
		$string = get_theme_mod('shareblock_s_no_post_to_load');
		if ( !empty( $string ) ){
			$string = $string;
		}else{
			$string = esc_html__( 'No post to load', 'shareblock' );
		}
		return $string;
	}

}
new shareblocktxt();